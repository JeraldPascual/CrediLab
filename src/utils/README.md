# Utils

Helper functions for API interaction.

| File | Purpose |
|---|---|
| `executeCode.js` | Sends code to `/api/execute-code` (Piston) with Firebase auth token. Handles rate-limit (429) errors. |
