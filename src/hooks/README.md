# Hooks

Custom React hooks.

| Hook | Purpose |
|---|---|
| `useAntiCheat.js` | Tracks focus-loss violations & copy-paste events during challenges. Persists state in localStorage. Auto-terminates after 3 violations. |
